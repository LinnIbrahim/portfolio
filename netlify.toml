[build]
  command = "npm run build"
  publish = "dist"

# Single-page portfolio — send any unknown path to index.html (HTTP 200)
# so deep links / refreshes resolve instead of 404ing.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
